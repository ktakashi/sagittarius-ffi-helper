INSTALL(FILES parser.scm
  DESTINATION ${SAGITTARIUS_SITE_LIB_DIR}/)
INSTALL(DIRECTORY parser
  DESTINATION ${SAGITTARIUS_SITE_LIB_DIR})
INSTALL(FILES preprocess.scm
  DESTINATION ${SAGITTARIUS_SITE_LIB_DIR}/)
INSTALL(DIRECTORY preprocess
  DESTINATION ${SAGITTARIUS_SITE_LIB_DIR})
